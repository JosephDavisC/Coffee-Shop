/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.unsplash.com',
        // 'abcxyz.supabase.co',
      ],
    },
  }
  
  module.exports = nextConfig